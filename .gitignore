node_modules
# Keep environment variables out of version control
.env
/node_modules
/contracheque exemplos
/src/generated/prisma
/erro
/uploads
/processados
*.tar
*.tar.gz
logs/
# Gerado por `npm run docs:export` (le os comentarios @swagger das rotas) -
# nunca editar a mao, e regenerado no CI antes do deploy pro GitHub Pages.
docs/openapi.json